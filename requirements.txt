# requirements.txt - Local testing version
# Core dependencies
requests>=2.31.0
python-dateutil>=2.8.0
fastapi>=0.104.0
uvicorn>=0.24.0

# Optional: For AWS deployment later
# boto3>=1.34.0
# strands-agents (when available)
# bedrock-agentcore (when available)

# Development and testing
pytest>=7.4.0